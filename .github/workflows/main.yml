name: Build Video Call Live V13 APK

on:
  workflow_dispatch:
  push:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.9'

      - name: Extract V13 project
        run: |
          unzip -q VideoCallLiveV13_PROFILE_FUNCTIONS.zip -d buildproject
          cp google-services.json buildproject/VideoCallLiveV13_PROFILE_FUNCTIONS/app/google-services.json
          ls -la buildproject/VideoCallLiveV13_PROFILE_FUNCTIONS

      - name: Build V13 APK
        working-directory: buildproject/VideoCallLiveV13_PROFILE_FUNCTIONS
        run: gradle :app:assembleDebug

      - name: Show SHA1
        working-directory: buildproject/VideoCallLiveV13_PROFILE_FUNCTIONS
        run: gradle signingReport

      - name: Upload V13 APK
        uses: actions/upload-artifact@v4
        with:
          name: VideoCallLive-V13-APK
          path: buildproject/VideoCallLiveV13_PROFILE_FUNCTIONS/app/build/outputs/apk/debug/app-debug.apk
