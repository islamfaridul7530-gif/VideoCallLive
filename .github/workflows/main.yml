name: Build Video Call Live V8 APK

on:
  workflow_dispatch:
  push:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.9'

      - name: Extract V8 project
        run: |
          unzip -q VideoCallLiveV8.zip -d buildproject
          cp google-services.json buildproject/VideoCallLiveV8/app/google-services.json
          ls -la buildproject/VideoCallLiveV8

      - name: Build V8 APK
        working-directory: buildproject/VideoCallLiveV8
        run: gradle :app:assembleDebug

      - name: Show SHA1
        working-directory: buildproject/VideoCallLiveV8
        run: gradle signingReport

      - name: Upload V8 APK
        uses: actions/upload-artifact@v4
        with:
          name: VideoCallLive-V8-APK
          path: buildproject/VideoCallLiveV8/app/build/outputs/apk/debug/app-debug.apk
