name: Build Video Call Live V14 APK

on:
  workflow_dispatch:
  push:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.9'

      - name: Extract V14 project
        run: |
          unzip -q VideoCallLiveV14_UI_PHOTO_FIX.zip -d buildproject
          cp google-services.json buildproject/VideoCallLiveV14_UI_PHOTO_FIX/app/google-services.json
          ls -la buildproject/VideoCallLiveV14_UI_PHOTO_FIX

      - name: Build V14 APK
        working-directory: buildproject/VideoCallLiveV14_UI_PHOTO_FIX
        run: gradle :app:assembleDebug

      - name: Show SHA1
        working-directory: buildproject/VideoCallLiveV14_UI_PHOTO_FIX
        run: gradle signingReport

      - name: Upload V14 APK
        uses: actions/upload-artifact@v4
        with:
          name: VideoCallLive-V14-APK
          path: buildproject/VideoCallLiveV14_UI_PHOTO_FIX/app/build/outputs/apk/debug/app-debug.apk
