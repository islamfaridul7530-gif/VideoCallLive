name: Build Video Call Live V18 APK

on:
  workflow_dispatch:
  push:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.9'

      - name: Extract V18 project
        run: |
          unzip -q VideoCallLiveV18_REAL_BANNER_TRIAL_FIX.zip -d buildproject
          cp google-services.json buildproject/VideoCallLiveV18_REAL_BANNER_TRIAL_FIX/app/google-services.json
          ls -la buildproject/VideoCallLiveV18_REAL_BANNER_TRIAL_FIX

      - name: Build V18 APK
        working-directory: buildproject/VideoCallLiveV18_REAL_BANNER_TRIAL_FIX
        run: gradle :app:assembleDebug

      - name: Show SHA1
        working-directory: buildproject/VideoCallLiveV18_REAL_BANNER_TRIAL_FIX
        run: gradle signingReport

      - name: Upload V18 APK
        uses: actions/upload-artifact@v4
        with:
          name: VideoCallLive-V18-APK
          path: buildproject/VideoCallLiveV18_REAL_BANNER_TRIAL_FIX/app/build/outputs/apk/debug/app-debug.apk
